
version: 2

models:
  - name: client
    description: "Client table as registered in the crm system"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
